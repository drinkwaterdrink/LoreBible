import {expect,test} from "bun:test";import {renderToString} from "react-dom/server";import {PhysicsStage} from "../../src/components/PhysicsStage";
const physics={density:"Standard",strangeness:3,mundanity:3,violence:"Moderate",horror:"Psych",romance:"Subplot",humor:"Dry",pacing:"Measured",explicitContent:"Fade",playerDeath:"Only if earned",linguisticBase:"",mustInclude:"",mustAvoid:""} as any;
test("Forge pacing can be continuous or one validated bundle at a time",()=>{const html=renderToString(<PhysicsStage physics={physics} onChangePhysics={()=>{}} onProceed={()=>{}} isCanonActive={false} chosenTitle="World" forgeExecutionMode="step_by_step" onChangeForgeExecutionMode={()=>{}}/>);expect(html).toContain("Forge execution");expect(html).toContain("Continuous");expect(html).toContain("One bundle at a time");expect(html).toContain("checked");});
